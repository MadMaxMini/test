#!/bin/bash
# notify-group.sh — DISABLED until Rod says go
# Group send confirmed working (chat ID routing). Re-enable when Rod approves.
echo "[notify-group] disabled — Rod said no group texts until further notice" >&2
exit 0
