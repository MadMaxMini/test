# Session 2026-05-14 — Google Sheets Auth Archaeology + Phase 2 Security Hardening

**Date:** 2026-05-14  
**Branch:** `main`  
**Key Event:** Located lost Google Sheets OAuth auth flow; updated Phase 2 hardening roadmap.

## What Happened

**Morning: Sheets API archaeology**
- Rod asked: "Where the hell is the Google Sheets API evidence? You can't find it."
- Spawned Explore agent to search git history, memory, filesystem for OAuth token cache + gspread imports
- Initial search found: Playwright-based OAuth at `~/.playwright-google/`, no gspread/google-auth installed, no service account creds
- Second search (looking at actual shell history): found the **one-time auth command**

**The Auth Command (Lost Until Today)**
```bash
python3 ~/Work/local/scripts/gsheets_login.py
```
- Opens browser window
- Rod logs in manually to Google
- Browser caches session to `~/.playwright-google/` (Chromium profile)
- `gsheets_sync.py` reuses that cached login for all Playwright writes
- OAuth token is browser-based, not service account
- Token still valid (last modified 2026-05-02)

**Security Review**
- Current: token/cookies stored in `~/.playwright-google/` unencrypted on disk
- Risk: low (mini is single-user, but credentials are not in vault)
- Rod raised: "Is storing the token in the script like that no bueno?"

**Phase 2 Roadmap Updated**
- Added item 6: "Credentials & auth hardening"
- Scope: move Playwright OAuth token to OpenBao vault (like HF_TOKEN pattern)
- Also audit other creds: property_map.json, sheet IDs, Dropbox access tokens
- Document credential lifecycle: create → store → rotate → revoke

## Key Decisions

1. **API vs Playwright confusion resolved**: No actual Google Sheets API yet. Phase 1 shipped with Playwright (8–15s/cell, browser-based). API re-wire is Phase 2 item if speed becomes a blocker (would need service account, `gspread` library, 0.1–0.5s/cell).
2. **Auth token remains on disk for now**: Phase 2 will move to OpenBao. Not urgent (mini is secure), but good hygiene.
3. **Idempotent writes explained**: Playwright writes same value = same state (cell gets value, not appended). Safe to retry without dedup logic.

## Commits

- `tasks/backlog.md` — updated P3 Sheets Phase 2 note with browser-OAuth details + token location
- `docs/sheets/roadmap.md` — added item 6 (credentials hardening) to Phase 2 scope

## Phase 2 Scope Expanded

**Added: Auto-detection layer (unified inbox + PDF classification)**
- Single inbox design: Sharon drops everything in one folder, system auto-classifies
- PDF field extractor: use `pdfplumber` to extract text, parse for reliable patterns
  - Org name, account/loan/property IDs, statement-specific fields
  - Return structured dict: `{org, account_id, statement_type, property, fields_found, confidence}`
- Statement classifier: deterministic pattern matching on extracted fields (not LLM)
  - Mortgage: loan number + "Loan Balance" + "Payment Due" + property
  - Chase Checking: "Chase" + "Checking" + account number + transaction columns
  - Chase Credit: "Chase" + "Credit Card" + account + transactions
  - PM Owner Statement: property name + "Owner Statement" + rent/expense fields
  - Unknown → quarantine + notify Rod
- CSV classifier: simple header matching (first 2 rows)
- Test matrix: 10 real samples (2-3 per type), measure 100% correctness
- Benefits: no wrong-folder drops, deterministic + auditable, extensible

## Final Decision: Phase Priority (2026-05-14, EOD)

**Account statements (Phase 1):** Working 100% as expected ✓  
**Cash tracking (Phase 3 pulled early):** Working code, not yet validated. No folder created yet.

**Decision: Do Phase 2 FIRST, then Phase 3 validation.**  
Rationale: Phase 2 will simplify folder structure + implement auto-detection. Once that visibility exists, Phase 3 validation becomes clearer.

**Phase 2 scope (merged to main):**
- Unified inbox design
- PDF field extractor + statement classifier (deterministic, not LLM)
- Credentials hardening (Playwright token → OpenBao)
- Asset inventory + flowchart + repo split audit
- Auto-detection layer

**Phase 3 (deferred until Phase 2):**
- Create cash tracking folder
- Validate bridge against June statements

**What's Next**
- Phase 2 design → build (flowchart, auto-detection, folder simplification)
- Phase 3 validation (after Phase 2 structure is in place)
