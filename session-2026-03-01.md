# Session Log — 2026-03-01

## Machine
Mac mini M4, 32GB, macOS Darwin 25.3.0 — dedicated Claude machine

---

## Completed This Session

### Environment Setup
- Confirmed specs: M4, 32GB (decided to keep vs 64GB upgrade — good value for solo dev)
- Established working directory structure: `~/Work/` (work files), `~/Work/local/` (local AI tooling)
- Created folder structure:
  ```
  ~/Work/local/
    ollama/
      modelfiles/
      scripts/
    open-webui/
  ```

### Ollama
- Installed Ollama 0.17.4 via install script
- Verified bound to `127.0.0.1:11434` (localhost only — secure default on macOS)
- Set `OLLAMA_HOST=127.0.0.1` in `~/.zshrc` for persistence
- No models pulled yet

### System Hardening
- SIP: enabled ✅
- FileVault: on ✅
- Firewall: enabled ✅
- Stealth mode: enabled (was off, turned on) ✅
- Port audit: only `symptomsd` on 53893 — legit Apple network diagnostics daemon, no action needed
- See [harden.md](harden.md) for full log and pending items

### Git / SSH
- Generated ed25519 SSH key for this machine
- Added to GitHub (MadMaxMini account)
- Configured `~/.ssh/config` for persistence
- Switched git remote from HTTPS to SSH
- Commits and pushes now working from terminal

### Docs Written
- [claude-permissions.md](claude-permissions.md) — open questions + action plan for Claude Code permissions and system hardening
- [local-ai.md](local-ai.md) — full local AI roadmap: trust tiers, model list, architecture, phased plan
- [harden.md](harden.md) — hardening log: what was done, what's pending, reference commands
- [session-2026-03-01.md](session-2026-03-01.md) — this file

---

## In Progress

### Homebrew
- Installing now via terminal
- Needed for: Python 3.12+, HuggingFace CLI, future tooling

---

## Up Next

### HuggingFace
- Create account at huggingface.co
- Generate access token (read + write)
- Install `huggingface-cli` via pip after brew + python sorted
- Use HF as primary model source (checksums, model cards, community vetting)

### First Model Pull
- Target: `Llama 3.3 70B (Q4)` or `Devstral 24B` via Ollama
- Verify weights via HuggingFace before pulling

### Claude Code Permissions
- Still need to sort autonomy/permission config
- See open questions in [claude-permissions.md](claude-permissions.md)

### Docker
- Needed for Tier 2 (DeepSeek, MiniMax) isolation
- `--network none` containers for untrusted models

---

## Decisions Made
| Topic | Decision |
|-------|----------|
| 32GB vs 64GB | Keep 32GB — good value, handles Tier 1 models well |
| Model trust tiers | Tier 1 native Ollama, Tier 2 Docker-isolated |
| Model source | HuggingFace primary, Ollama registry for convenience |
| Git auth | SSH keys (not HTTPS) |
| Ollama binding | localhost only, locked in env |
| Chinese models | Open to exploring, Docker-gated for isolation |
| Memory files | System-wide memory only on explicit request, project files in project folder |
